import { h, Component } from 'preact'

export default class NAME extends Component {
  constructor(props) {
    super(props)
  }
  render(props, state) {
    return(
      <div>NAME</div>
    )
  }
}

const NAME = (props) => {

  return(
    <div>NAME</div>
  )
}
export default NAME
