import { h, Component } from 'preact';
import {
  LoaderContainer,
  LoaderIcon
} from './Style.js';

const LoadingBody = () => (
  <LoaderContainer>
    <LoaderIcon />
  </LoaderContainer>
)

export default LoadingBody;
